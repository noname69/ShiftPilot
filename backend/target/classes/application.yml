spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # dev only (use validate in prod)
    # show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

frontend:
  url: http://localhost:5173

logbook:
  filter:
    enabled: false
  obfuscate:
    headers:
      - Authorization
      - Cookie
    parameters:
      - password
  exclude:
    - /actuator/**
  write:
    max-body-size: 10000

logging:
  level:
    org.zalando.logbook: TRACE

#spring:
#  datasource:
#    url: jdbc:postgresql://localhost:5432/shift_pilot
#    username: postgres
#    password: password
#    driver-class-name: org.postgresql.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#    database-platform: org.hibernate.dialect.PostgreSQLDialect
#
#server:
#  port: 8080
#
#frontend:
#  url: http://localhost:5173